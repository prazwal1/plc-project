# Static typing with type inference -- no explicit declarations needed.
# The type is fixed on first assignment.

age    = 25;
height = 1.75;
active = true;
name   = "Alice";

print(age);
print(height);
print(active);
print(name);
